# Project Overview

This project is a simple Python script that generates the Fibonacci sequence up to a given number. The script includes a function `fibonacci(n)` which calculates and returns the sequence, and a main block that prints the Fibonacci sequence up to 100.

## File Structure
- `fibonacci.py`: Contains the Fibonacci sequence generator and the main execution block.